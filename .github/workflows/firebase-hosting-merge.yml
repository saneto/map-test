# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
      - uses: actions/checkout@v4
      - name: Show exported environment variables
        run: ls src/
      - name: Create src/environment directory
        run: mkdir -p src/environments
      - name: Show exported environment variables
        run: ls src/
      - name: Show exported environment variables
        run: echo "'${{vars.FIREBASE_VALUE}}'"
      - name: Show exported environment variables
        run: echo "$FIREBASE_VALUE"
      - name: Show exported environment variables
        run: echo "${{env.FIREBASE_VALUE}}"
      - name: Export environment variables
        run: echo "export const environment ='${{ secrets.FIREBASE_VALUE }}';" > src/environments/environment.ts
      - name: Show exported environment variables
        run: cat src/environments/environment.ts
      - run: ls && npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TEST_F08F0 }}'
          channelId: live
          projectId: test-f08f0
